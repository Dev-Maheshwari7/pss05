window.addEventListener('DOMContentLoaded',()=>{
 const D=DDAS_DATA, prevented=D.activities.filter(a=>a.type==='prevented').length, overrides=D.activities.filter(a=>a.type==='override').length, requests=D.activities.filter(a=>a.type==='request').length;
 document.getElementById('activityMetrics').innerHTML=[ddas.metric('Events today','1,284','Across all repositories'),ddas.metric('Duplicates prevented',prevented+' recent','829 lifetime'),ddas.metric('Pending access requests',requests,'Median approval: 18 min'),ddas.metric('Override rate','3.8%',overrides+' shown in demo')].join('');
 const list=document.getElementById('auditList');
 function render(type='all'){const rows=D.activities.filter(a=>type==='all'||a.type===type);list.innerHTML=rows.map(a=>`<div class="audit-row clay-inset"><div class="activity-icon clay-soft">${a.icon}</div><div><h3>${a.title}</h3><p>${a.detail}</p></div><div class="audit-meta"><b>${a.user}</b><br>${a.saved!=='—'?'Saved '+a.saved:'No saving logged'}</div><div class="audit-meta">${a.time} ago</div></div>`).join('')}
 document.querySelectorAll('.activity-filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.activity-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.type)});render();
 document.getElementById('exportBtn').onclick=()=>{const rows=[['type','title','detail','user','time','saved'],...D.activities.map(a=>[a.type,a.title,a.detail,a.user,a.time,a.saved])];const csv=rows.map(r=>r.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='ddas-audit-demo.csv';a.click();URL.revokeObjectURL(a.href);ddas.toast('Audit CSV exported ✓')};
});
